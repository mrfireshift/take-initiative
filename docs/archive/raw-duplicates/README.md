# Raw duplicates archiviati

Questi file non sono più mantenuti sotto `docs/class-features/raw/` perché
sono copie byte-per-byte di artefatti in `data/class-features/`, oppure un
pacchetto (`module.zip`) senza riferimenti nel runtime.

I 14 JSON sono stati confrontati con SHA-256 prima dello spostamento. La fonte
canonica resta `data/class-features/`; generatori e test non leggono questo
archivio.

File conservati:

- `phb2014_livello_meccanico_schema_v1_1.json`;
- i sei file `ranger_revised_*` verificati;
- i due file `tasha_livello_meccanico_*` verificati;
- i cinque file `xanathar_*` verificati;
- `module.zip`.
